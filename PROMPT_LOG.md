# Prompt Log — Лабораторная работа №10, Вариант 9

## Задание М1: 1. Создать простое API на Go (Gin) с 2-3 эндпоинтами

### Промпт 1

**Инструмент:** DeepSeek

**Промпт:**
```
"Создай REST API на Go для управления задачами.Используй Gin framework, хранилище в 
памяти с мьютексами.
Нужны эндпоинты:
- POST /api/tasks — создать задачу
- GET /api/tasks — получить все задачи
- GET /api/tasks/:id — получить задачу
- PUT /api/tasks/:id — обновить задачу
- DELETE /api/tasks/:id — удалить задачу
- GET /health — проверка здоровья

Реализуй валидацию входных данных (title обязателен, min 3, max 100; status должен быть
одним из pending/in-progress/done).
Добавь middleware для логирования запросов (метод, путь, статус, время).
```

**Результат:** Была получена реализация API с эндпоинтами. Отсутствует go.mod.

### Промпт 2

**Инструмент:** DeepSeek

**Промпт:**
```
Напиши тесты для API. Используй httptest. Создай функцию setupRouter() для тестового
окружения. Добавь go.mod.
```

**Результат:** Получены тесты для проверки. Все работает, тесты проходит

### Итого

- Количество промптов: 2
- Что пришлось исправлять вручную: ничего
- Время: ~25 мин

---

## Задание М2: Реализовать валидацию входных данных в Go

### Промпт 1

**Инструмент:** DeepSeek

**Промпт:**
```
Создай валидатор на Go для проверки входных данных. Нужно реализовать:
- Пользовательские валидаторы: username (3-20 символов, alphanumeric + underscore),
password (min 8 chars, uppercase, lowercase, digit, special char), email_strict (с
проверкой формата и запретом двойных точек), phone (международный формат), age_range
(с параметрами min,max), date_format (YYYY-MM-DD)
- Структуру User со всеми необходимыми полями и тегами валидации
- Функцию ValidateUser, возвращающую структурированный результат с ошибками
- Отдельные функции для валидации каждого поля
- Table-driven тесты с subtests для всех валидаций
- Бенчмарки для измерения производительности
```

**Результат:** Получена полная реализация валидатора

### Промпт 2

**Инструмент:** DeepSeek

**Промпт:**
```
Добавь обработку ошибок в ValidateUser для всех типов валидационных ошибок. Убедись, что
все ошибки проверяются и обрабатываются корректно.
```

**Результат:** Все работает, тесты проходит

### Итого

- Количество промптов: 2
- Что пришлось исправлять вручную: ничего
- Время: ~30 мин

---

## Задание М3: Передавать сложные структуры данных (JSON) между сервисами

### Промпт 1

**Инструмент:** DeepSeek

**Промпт:**
```
Создай FastAPI сервис (порт 8000) и Gin сервис (порт 8080) на Go, которые обмениваются 
JSON-структурами. FastAPI получает запросы и проксирует их в Gin через асинхронный HTTP 
клиент. Gin хранит заказы in-memory.
Модель Order: order_id, customer_name, customer_email, address (street, city, country, 
zip_code), products (id, name, price, quantity).
Эндпоинты: POST /orders, GET /orders/{id}, GET /orders, GET /health.
Оба сервиса должны иметь middleware для логирования запросов и health check.
```

**Результат:** 
- Получена полная реализация Gin сервиса с моделями и эндпоинтами. 
- Получен FastAPI сервис с клиентом для Gin

### Промпт 2

**Инструмент:** DeepSeek

**Промпт:**
```
Напиши юнит-тесты для обоих сервисов. Тесты должны использовать моки для HTTP-запросов 
между сервисами.
```

**Результат:** Получены юнит-тесты. Все работает, тесты проходит

### Итого

- Количество промптов: 2
- Что пришлось исправлять вручную: ничего
- Время: ~30 мин

---

## Задание h1: Добавить аутентификацию (JWT) в Go-сервисе и проверять токены из Python + Развернуть оба сервиса в Docker Compose с общей сетью. 

### Промпт 1

**Инструмент:** DeepSeek

**Промпт:**
```
Создай два микросервиса: Go-сервис с JWT аутентификацией (эндпоинты /login, /public, 
/protected/data) и Python-сервис-клиент на FastAPI, который проксирует запросы к Go и 
локально валидирует JWT. Используй общий секретный ключ "my-secret-key". Go хранит 
пользователя admin/password123 в коде.
```

**Результат:**  Получено два работающих микросервиса с JWT-аутентификацией

### Промпт 2

**Инструмент:** DeepSeek

**Промпт:**
```
Напиши юнит-тесты для Go-сервиса: тестирование генерации и валидации JWT, middleware 
авторизации и эндпоинтов /login и /protected. Для Python-сервиса напиши тесты для 
jwt_validator (валидный, просроченный, невалидный токен) и для эндпоинтов FastAPI с 
моком httpx клиента.
```

**Результат:**  Получен набор тестов

### Промпт 3

**Инструмент:** DeepSeek

**Промпт:**
```
Добавь pyproject.toml для Python проекта с зависимостями и настройками тестов. Обнови 
go.mod с необходимыми зависимостями и остальные файлы, если надо.
```

**Результат:**  Получен pyproject.toml

### Промпт 4

**Инструмент:** DeepSeek

**Промпт:**
```
Напиши Dockerfile для Go-сервиса (многоступенчатая сборка) и для Python-сервиса. Создай 
docker-compose.yml с общей сетью, где оба сервиса запускаются, Go на порту 8080, Python 
на порту 8000. Python должен обращаться к Go по имени сервиса.
```

**Результат:**  Получены контейнеризированные сервисы с общей сетью

### Итого

- Количество промптов: 4
- Что пришлось исправлять вручную: ничего
- Время: ~50 мин

---